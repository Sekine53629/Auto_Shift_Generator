# 変更履歴

> v1〜v3 はクリック入力マクロ1本だけだった時期の版番号です。
> その後モジュールが分かれ、版はモジュールごとに持つようになりました。
> 現在の各モジュールの版は [README](README.md#モジュール構成) の表を参照してください。
> 以降は日付で区切って記録します。

## 2026-08-27 — 全体リファクタリングと配置アルゴリズムの修正（[PR #6](https://github.com/Sekine53629/Auto_Shift_Generator/pull/6)）

### 修正 — シフト配置

- **医師が多い日ほど薬剤師が薄くなる逆転を解消。** 5診日の平均出勤が4診日を下回っていた
  - `FiveBalance` が5診日の出勤を減らし続けていた。個人ごとの5診出勤回数を均すとき
    「最多の人を降ろす」分岐を先に試し、成功するとその巡回を終えていたため、
    成功のたびに5診日の総出勤が1名ずつ減っていた
  - 日別の過不足を均す工程が無かった → `CoverBalance` を追加
  - 連勤・連休の判定を交換**前**の盤面で行っていたため、抜く日が入れる日の隣にあると
    実際には収まる交換まで弾いていた → 入れ替えてから判定し必ず戻す形に変更
- **`AS_連勤緩和` が勤務ルールを見ずに全員へ `RepairRuns` をかけていた。**
  固定曜日の曜日契約と週N日の週あたり勤務日数を崩し得る状態だった。
  固定曜日は対象外にし、週N日は同じ週の中だけで入れ替える
- **事務員の記号割当。** 2人目に `●` を置くときも `○` の回数で候補を選んでいた
- **記号の候補選択が同点のとき常に上の行を選び、1人に偏っていた。**
  日ごとに走査開始位置をずらして解消（乱数ではなく巡回。同じ入力なら同じ結果になる）

同一入力での実測（2026年8月・薬剤師10名／事務員2名）:

| | 修正前 | 修正後 |
|---|---|---|
| 5診日 平均出勤 | 4.25 | 4.75 |
| 4診日 平均出勤 | 4.67 | 4.33 |
| 診数の序列 | 逆転 | 正常 |
| 必要数に届かない日 | 16日 | 8日 |
| うち2名不足の日 | 4日 | 0日 |

### 追加 — 設定（いずれも既定値では従来どおりの動作）

- `事務員の早番(○) 人数/日` — コードに直書きしていた1を設定へ出した
- `混雑日（医師5名）の遅番(▲) 最低人数/日` — 5診日だけ遅番を厚くできる
- `不足を埋めるときの連勤上限の上乗せ` — その交換のときだけ連勤の上限を緩める。
  1にすると必要数不足が8日→4日に減る代わりに4連勤が発生するため、労務上の判断が要る。
  使ったときは結果ダイアログに誰が何日連勤になったかを列挙する

### 追加 — レポート・ログ

- **人日収支** — 必要数に届かない日があるとき、月の総必要人日と総出勤人日を並べる。
  「配分の偏り」か「人手不足」かで打ち手が変わるため
- **読まれない設定の警告** — 月間休日数は勤務ルールが `通常` の人しか読まない。
  固定曜日・週N日の人に入れても黙って捨てられていたので、実行前に一覧で出す
- **`CoverBalance` の診断ログ** — 評価値・手数・詰まった箇所を `C:\VBAErrorLogs\` に残す

### 追加 — 機能

- **印刷用の出力（`ShiftExport`）** — 年月行〜過不足行 / A〜AL列を切り出し、数式は値に、
  条件付き書式の色は静的な塗りに焼き込む。PDF / Excel を実行時に選択
- **祝日マスタの Power Query 取り込み** — 内閣府の CSV を取り込み、以後は右クリック→更新で最新に
- **パレットに `戻す` / `出力` ボタンを追加**
- **パレット用の3行をセットアップ時に自動挿入** — 白紙のブックからでも作れる
- **休業者の行を灰色（#BFBFBF）に** — マクロが塗った色と同じ場合だけ外すので、
  背景色ボタンで付けた色は消えない
- **書き込み先を3領域に専用化** — シフト入力欄／備考行／医師名欄でそれぞれ押せるスタンプを限定

### 修正 — その他

- `AS_レポート` が常に `False` を返し、最終の `LogSuccess` に到達していなかった
- 医師名スタンプの判定が一度も成立していなかった（`医師名` で前方一致を見ていたが
  パレットに書かれるのは `医師`）。備考スタンプも道連れで死んでいた
- 期替わり判定が一度も発火していなかった（年月セルが数式のため `Change` が起きない）
- スタンプがパレットの色を持ち込んでいた
- 出力がエラー1004で失敗する／土日祝の色が消える
- 変数名 `cDate` が組み込み関数 `CDate` と衝突していた
- 名前付き範囲の名前と中身が食い違っていた（`シフトパレット範囲` → `シフト入力範囲`）

### 内部

- 定数を `ShiftCommon` に集約。`"薬剤師"` の二重定義を統合し、直書きの記号を定数化
- 長い手続きを工程ごとに分割（`AS_記号割当` 136行 → 7手続き ほか）
- 静的チェック `tools/check_vba.py` を追加し CI に組み込み（12検査）。
  検査8〜12 はいずれも実際に混入したミスを受けて追加したもの
- `build_sjis.py` を `src/*.bas` の自動列挙に変更し、出力を CRLF に正規化

---

## v3 — 2026-08-20
### 変更
- **ダブルクリック方式に変更。** セルを選ぶだけのクリックでは値が変わらなくなりました
- `CYCLE_TRIGGER` / `STAMP_TRIGGER` で `"double"` / `"single"` を切り替え可能に
- スタンプモードで「範囲選択 → 右クリック」による一括入力に対応
### 追加
- `ShiftClick_選択範囲にスタンプ`（ショートカットキー割り当て向け）
- `ShiftClick_セルフチェック`（設定の確認用）
### 内部
- `ShiftClick_Handle` の引数を `EventKind`（`select` / `double` / `right`）に変更
  → シートモジュールも差し替えが必要

## v2 — 2026-08-20
### 修正
- `ROW_OFF` などの定数宣言がプロシージャより後ろにあり、
  「プロシージャの外では無効です」になる不具合を修正
- 同定数を `Private` から `Public` に変更（別モジュールからの参照エラーを回避）
- コピー＆ペースト用に `Attribute VB_Name` 行を除いたファイルを追加

## v1 — 2026-08-20
- 初版。クリックで `空白 → ◯ → ● → ▲ → 公休 → 希休` を巡回
- 右クリックで逆順
- スタンプパレット（パレットのセルの書式ごとコピー、緑地の ◯ ● に対応）
